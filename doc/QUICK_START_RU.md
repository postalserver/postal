# Быстрый старт - Разработка и развертывание Postal на Ubuntu

## 🚀 Три простых сценария использования

### 1️⃣ Локальная разработка (на вашем компьютере)

Идеально для тестирования изменений перед отправкой на сервер.

```bash
# Один раз - настройка окружения
./script/dev-setup.sh

# Создать администратора
./bin/postal make-user

# Запустить для разработки (все процессы сразу)
foreman start -f Procfile.dev

# Или запустить процессы по отдельности в разных терминалах:
./bin/postal web-server    # http://localhost:5000
./bin/postal smtp-server   # порт 2525
./bin/postal worker
```

**Тестирование:**
```bash
bundle exec rspec              # Запустить все тесты
./bin/postal console           # Rails консоль
```

### 2️⃣ Первоначальная установка на сервер Ubuntu 22.04

Выполняется один раз при первой установке.

```bash
# На сервере
sudo ./script/ubuntu-install.sh

# Следуйте инструкциям скрипта
# Он установит все зависимости, настроит БД и запустит сервисы
```

### 3️⃣ Быстрое обновление на сервере (главная фишка!)

После того, как вы (или Claude) внесли изменения в код и запушили в git:

```bash
# На сервере - всего одна команда!
cd /opt/postal/app
./script/deploy-update.sh
```

**Вот и всё!** Скрипт автоматически:
- ✅ Скачает изменения из git
- ✅ Обновит зависимости
- ✅ Выполнит миграции БД
- ✅ Перезапустит сервисы
- ✅ Проверит, что всё работает

---

## 📋 Типичный рабочий процесс

### Когда Claude делает изменения в коде:

**Раньше (с Docker):**
```bash
1. Claude: коммит → push
2. Вы: git merge
3. Вы: docker build (долго, 5-10 минут...)
4. Вы: docker-compose down
5. Вы: docker-compose up
6. Надеяться что всё работает
```

**Теперь (native Ubuntu):**
```bash
1. Claude: коммит → push
2. Вы: ./script/deploy-update.sh
3. Готово! (30-60 секунд)
```

### Если что-то пошло не так:

```bash
# Откат к предыдущей версии (мгновенно!)
cd /opt/postal/app
git log --oneline -10           # Посмотреть историю
git checkout ПРЕДЫДУЩИЙ_КОММИТ  # Откатиться
./script/deploy-update.sh       # Применить откат
```

---

## 🔧 Полезные команды для сервера

### Управление сервисами:
```bash
# Проверить статус
sudo systemctl status postal-*

# Перезапустить всё
sudo systemctl restart postal-web postal-smtp postal-worker

# Остановить
sudo systemctl stop postal-*

# Запустить
sudo systemctl start postal-*
```

### Просмотр логов:
```bash
# Все логи вместе (в реальном времени)
sudo journalctl -u postal-* -f

# Только web сервер
sudo journalctl -u postal-web -f

# Только SMTP
sudo journalctl -u postal-smtp -f

# Только worker
sudo journalctl -u postal-worker -f

# Последние 100 строк
sudo journalctl -u postal-* -n 100
```

### Работа с базой данных:
```bash
# Консоль Rails (для отладки)
cd /opt/postal/app
./bin/postal console

# Прямой доступ к MySQL
mysql -u postal -p postal

# Бэкап базы данных
mysqldump -u postal -p postal > postal_backup_$(date +%Y%m%d).sql

# Восстановление
mysql -u postal -p postal < postal_backup_20241130.sql
```

---

## 🎯 Сравнение подходов

| Действие | Docker | Native Ubuntu | Выигрыш |
|----------|--------|---------------|---------|
| Обновление кода | 5-10 мин | 30-60 сек | **10x быстрее** |
| Просмотр логов | `docker logs` | `journalctl` | Удобнее |
| Отладка | Внутри контейнера | Напрямую | Проще |
| Откат версии | Пересборка | `git checkout` | Мгновенно |
| Использование памяти | ~500-800 MB | ~200-300 MB | Экономия |
| Изменение конфига | Пересборка | Редактировать файл | Проще |

---

## 📁 Структура файлов

```
/opt/postal/
├── app/                          # Код Postal (git repo)
│   ├── bin/postal               # Главный исполняемый файл
│   ├── script/
│   │   ├── ubuntu-install.sh    # Первая установка
│   │   ├── deploy-update.sh     # Быстрое обновление ⭐
│   │   └── dev-setup.sh         # Настройка для разработки
│   └── doc/
│       ├── UBUNTU_DEPLOYMENT.md # Полная документация
│       └── QUICK_START_RU.md    # Этот файл
└── config/
    └── postal.yml               # Конфигурация
```

---

## 🔐 Безопасность

### Рекомендуемые настройки:

```bash
# Firewall (UFW)
sudo ufw allow 22/tcp     # SSH
sudo ufw allow 25/tcp     # SMTP
sudo ufw allow 80/tcp     # HTTP
sudo ufw allow 443/tcp    # HTTPS
sudo ufw enable

# Автоматические обновления безопасности
sudo apt-get install unattended-upgrades
sudo dpkg-reconfigure -plow unattended-upgrades

# Регулярные бэкапы БД (добавить в crontab)
0 2 * * * mysqldump -u postal -pПАРОЛЬ postal > /opt/postal/backups/postal_$(date +\%Y\%m\%d).sql
```

### SSL/TLS сертификаты (Let's Encrypt):

```bash
# Установить certbot
sudo apt-get install certbot

# Получить сертификат
sudo certbot certonly --standalone -d mail.example.com

# Обновить postal.yml
# tls_certificate_path: /etc/letsencrypt/live/mail.example.com/fullchain.pem
# tls_private_key_path: /etc/letsencrypt/live/mail.example.com/privkey.pem
```

---

## ❓ FAQ

**Q: Можно ли использовать Docker и native одновременно?**
A: Да, но не рекомендуется. Выберите один подход для упрощения.

**Q: Как часто нужно делать обновления?**
A: Используйте `./script/deploy-update.sh` каждый раз, когда появляются изменения в git.

**Q: Что делать если обновление сломало систему?**
A: Откатитесь к предыдущему коммиту и запустите `deploy-update.sh` снова.

**Q: Можно ли запускать на Ubuntu 20.04 или 24.04?**
A: Да, скрипты должны работать, но тестировались на Ubuntu 22.04.

**Q: Нужны ли права root?**
A: Для первой установки - да. Для обновлений - можно от пользователя postal, но перезапуск сервисов требует sudo.

---

## 📞 Дополнительная помощь

- Полная документация: `doc/UBUNTU_DEPLOYMENT.md`
- Официальная документация Postal: https://docs.postalserver.io
- GitHub Issues: https://github.com/postalserver/postal/issues
- Discord: https://discord.postalserver.io

---

## ✨ Итого

С новым подходом вы получаете:

1. **Быструю разработку**: изменения тестируются локально за секунды
2. **Простое развертывание**: одна команда вместо 5-6 шагов
3. **Легкую отладку**: прямой доступ к коду и логам
4. **Быстрый откат**: git checkout вместо пересборки образов
5. **Меньше памяти**: нет оверхеда Docker

**Основная команда которую нужно запомнить:**
```bash
./script/deploy-update.sh
```

Вот и всё! 🎉
