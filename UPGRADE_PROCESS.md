# Обновление Postal после получения новых изменений

## Проблема
После обновления кода из GitHub (например, после `git pull` или переустановки из обновленного репозитория), новые изменения в базе данных не применяются автоматически.

## Правильный процесс обновления

### 1. Остановите Postal
```bash
postal stop
```

### 2. Обновите код
```bash
cd /home/user/postal  # или где находится ваш репозиторий
git pull origin main
```

### 3. Примените миграции базы данных
```bash
postal upgrade
```

Эта команда:
- Проверит наличие новых миграций
- Выполнит все новые миграции автоматически
- Обновит схему базы данных

### 4. Перезапустите Postal
```bash
postal start
```

## Быстрое исправление для текущей проблемы

Если вы уже обновили код, но забыли запустить `postal upgrade`:

```bash
postal upgrade
postal restart  # или postal stop && postal start
```

## Автоматизация

Создайте скрипт для обновления `update-postal.sh`:

```bash
#!/bin/bash
set -e

echo "🛑 Stopping Postal..."
postal stop

echo "📥 Pulling latest changes..."
cd /home/user/postal
git pull origin main

echo "🔄 Running database migrations..."
postal upgrade

echo "✅ Starting Postal..."
postal start

echo "✨ Update complete!"
```

Сделайте его исполняемым:
```bash
chmod +x update-postal.sh
```

Используйте при каждом обновлении:
```bash
./update-postal.sh
```

## Проверка версии

Проверьте текущую версию Postal:
```bash
postal version
```

## Проверка миграций

Проверьте, какие миграции применены:
```bash
cd /home/user/postal
bundle exec rails db:migrate:status
```

Или через Docker (если используете контейнеры):
```bash
docker compose exec web bash -c "cd /opt/postal/app && bundle exec rails db:migrate:status"
```

## Важно

⚠️ **ВСЕГДА** запускайте `postal upgrade` после обновления кода из Git!

Это гарантирует, что все изменения базы данных будут применены корректно.
