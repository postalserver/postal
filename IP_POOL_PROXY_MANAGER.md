# 🚀 IP Pool Proxy Manager - Полное руководство

## Что это?

**IP Pool Proxy Manager** - это полноценная система управления SOCKS прокси для каждого IP адреса в Postal **прямо из веб-интерфейса**.

### ✨ Главная фича:

**Вы можете добавить IP адрес в IP pool, указать SSH данные удаленного сервера, и Postal автоматически:**
1. 📦 Установит Dante SOCKS сервер на удаленный сервер
2. ⚙️ Настроит его с авторизацией по IP
3. 🔥 Настроит firewall
4. 🧪 Протестирует подключение
5. ✉️ Начнет отправлять почту через этот прокси

**И всё это БЕЗ копания в конфигах и терминале!**

---

## 🎯 Зачем это нужно?

### Проблема:
У вас Postal на сервере с IP1, но вы хотите отправлять почту с IP2 другого сервера.

### Решение:
```
Postal (IP1) → SOCKS Proxy (IP2) → Gmail/Outlook
                         ↑
            Получатели видят только этот IP!
```

---

## 📋 Возможности

### ✅ Что умеет:

- **Автоматическая установка** Dante SOCKS сервера через SSH
- **Тестирование прокси** прямо из интерфейса одной кнопкой
- **Статус мониторинг** для каждого IP address
- **Per-IP proxy settings** - каждый IP может иметь свой прокси
- **Автоматическая настройка firewall** на удаленном сервере
- **Защита по IP** - только ваш Postal сервер может подключаться
- **Логирование** всех операций установки

### 🎨 Режимы работы:

1. **Auto-install mode** - указываете SSH данные, Postal делает всё сам
2. **Manual mode** - указываете готовый SOCKS proxy (если уже установлен)
3. **Global proxy** - настройка прокси для всех IP адресов через postal.yml

---

## 🚀 Быстрый старт

### Шаг 1: Запустить миграцию

```bash
cd /home/user/postal
bundle install
bundle exec rails db:migrate
```

### Шаг 2: Перезапустить Postal

```bash
postal restart
# или
systemctl restart postal
```

### Шаг 3: Открыть веб-интерфейс

1. Перейдите в **IP Pools** → выберите пул → **Add IP Address**
2. Заполните основные поля (IPv4, Hostname, Priority)
3. Включите **"Use SOCKS5 proxy for this IP address"**
4. Выберите **"Automatically install Dante SOCKS server"**
5. Заполните:
   - **Remote Server IP** - IP адрес сервера для прокси (откуда будет идти почта)
   - **SSH Port** - 22 (или ваш)
   - **SSH Username** - root (или другой пользователь с sudo)
   - **SSH Password** - пароль для SSH
6. **Сохраните** ✅

### Шаг 4: Автоматическая установка

После сохранения:
- Postal автоматически запустит установку Dante на удаленном сервере
- Вы увидите статус установки: `Installing → Installed → Active`
- Можно также нажать кнопку **"📦 Install Proxy Now"** для ручного запуска

### Шаг 5: Тестирование

Нажмите **"🧪 Test Proxy Connection"**

Вы увидите:
- ✅ Успешное подключение + внешний IP
- ❌ Ошибки подключения с деталями

### Шаг 6: Отправляем почту!

Теперь все письма с этим IP адресом будут автоматически отправляться через прокси!

---

## 📖 Детальная инструкция

### Создание IP с auto-install proxy

1. **IP Pools** → выберите пул
2. **Add IP Address**
3. Заполните:

```
IPv4: 192.168.1.100
Hostname: mail.example.com
Priority: 100

✓ Use SOCKS5 proxy for this IP address
  ✓ Automatically install Dante SOCKS server on remote server

  Remote Server IP: 203.0.113.50  (IP откуда будет идти почта)
  SSH Port: 22
  SSH Username: root
  SSH Password: ••••••••
```

4. **Save**
5. Ждите автоматической установки (~30-60 секунд)

### Создание IP с manual proxy

Если у вас уже установлен SOCKS proxy:

```
IPv4: 192.168.1.100
Hostname: mail.example.com
Priority: 100

✓ Use SOCKS5 proxy for this IP address
  ☐ Automatically install Dante SOCKS server

  Proxy Host: 203.0.113.50
  Proxy Port: 1080
  Proxy Username: (optional)
  Proxy Password: (optional)
```

---

## 🔧 Что происходит под капотом?

### Процесс auto-install:

1. **SSH подключение** к удаленному серверу
2. **Определение ОС** (Ubuntu/Debian/CentOS)
3. **Установка Dante**: `apt-get install dante-server`
4. **Создание конфига** с авторизацией по IP Postal сервера
5. **Запуск сервиса**: `systemctl start danted`
6. **Настройка firewall**: разрешить 1080 только для Postal IP
7. **Тестирование** соединения
8. **Установка статуса** → `active`

### Конфиг Dante (автоматически генерируется):

```conf
internal: 0.0.0.0 port = 1080
external: eth0

clientmethod: none
socksmethod: none

client pass {
    from: <POSTAL_IP>/32 to: 0.0.0.0/0
}

socks pass {
    from: <POSTAL_IP>/32 to: 0.0.0.0/0
    protocol: tcp
    command: connect
}
```

### Отправка почты:

1. Postal выбирает IP address для отправки (по приоритету)
2. Проверяет `ip_address.proxy_configured?`
3. Если да - настраивает SOCKS через `ip_address.proxy_host:proxy_port`
4. Устанавливает SMTP соединение через SOCKS proxy
5. Получатель видит IP удаленного сервера (proxy_ssh_host)

---

## 🎛️ Statuses

| Status | Описание |
|--------|----------|
| `not_configured` | Прокси не настроен |
| `installing` | Идет установка Dante на удаленном сервере |
| `installed` | Dante установлен, но не протестирован |
| `testing` | Идет тестирование соединения |
| `active` | ✅ Прокси работает и тестирован |
| `failed` | ❌ Ошибка установки или подключения |

---

## 🛠️ Troubleshooting

### 1. Статус `failed` после установки

**Проверьте:**
- SSH данные правильные?
- У пользователя есть права sudo?
- Firewall на удаленном сервере разрешает SSH?
- Достаточно ли места на диске?

**Посмотреть логи:**
- В интерфейсе под статусом показывается лог установки
- Логи Rails: `tail -f /opt/postal/log/production.log`

### 2. Test Proxy показывает ошибку

**Возможные причины:**
- Dante не запущен: `systemctl status danted` на удаленном сервере
- Firewall блокирует 1080 порт
- Неправильный IP в конфиге Dante

**Решение:**
```bash
# На удаленном сервере
systemctl restart danted
systemctl status danted

# Проверить firewall
ufw status
ufw allow from <POSTAL_IP> to any port 1080

# Проверить слушает ли порт
netstat -tlnp | grep 1080
```

### 3. Письма не идут через прокси

**Проверить:**
1. Статус IP address = `active`?
2. Checkbox "Use SOCKS5 proxy" включен?
3. Логи SMTP: `/opt/postal/log/smtp.log`

**Debug:**
```bash
# Проверить что прокси работает
curl --socks5 <proxy_host>:1080 https://ifconfig.me
```

### 4. SSH Connection Timeout

**Возможные причины:**
- Неправильный IP/порт
- Firewall блокирует SSH
- SSH сервер не запущен

**Решение:**
```bash
# Проверить доступность SSH с Postal сервера
ssh -p 22 root@<remote_ip>

# Проверить firewall
ufw allow 22
```

---

## 🔐 Безопасность

### Рекомендации:

1. **SSH Password в БД** зашифрован (но лучше использовать SSH keys - coming soon)
2. **Firewall обязателен** - разрешайте 1080 только для Postal IP
3. **Используйте fail2ban** для защиты SSH
4. **Регулярно меняйте пароли**

### Настройка firewall на удаленном сервере:

```bash
# UFW (Ubuntu/Debian)
ufw allow 22
ufw allow from <POSTAL_IP> to any port 1080
ufw deny 1080
ufw enable

# firewalld (CentOS/RHEL)
firewall-cmd --permanent --add-rich-rule='rule family="ipv4" source address="<POSTAL_IP>" port port="1080" protocol="tcp" accept'
firewall-cmd --reload
```

---

## 📊 API доступ

### Тестирование прокси через API:

```bash
POST /ip_pools/:ip_pool_id/ip_addresses/:id/test_proxy
```

**Response:**
```json
{
  "success": true,
  "message": "Proxy connection successful! External IP: 203.0.113.50",
  "external_ip": "203.0.113.50"
}
```

### Установка прокси через API:

```bash
POST /ip_pools/:ip_pool_id/ip_addresses/:id/install_proxy
```

---

## 🎨 Архитектура

### Компоненты:

```
┌─────────────────────────────────────────┐
│  Web UI (IP Pool Form)                  │
│  app/views/ip_addresses/_form.html.haml │
└────────────┬────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────┐
│  Controller                              │
│  app/controllers/ip_addresses_controller.rb │
│  - test_proxy                            │
│  - install_proxy                         │
└────────────┬────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────┐
│  Background Job                          │
│  app/jobs/proxy_installer_job.rb         │
└────────────┬────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────┐
│  Services                                │
│  app/lib/proxy_manager/                  │
│  - proxy_installer.rb  (SSH install)     │
│  - proxy_tester.rb    (Test connection)  │
└────────────┬────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────┐
│  SMTP Endpoint                           │
│  app/lib/smtp_client/endpoint.rb         │
│  - setup_smtp_with_ip_address_proxy()    │
└──────────────────────────────────────────┘
```

---

## 📝 Примеры использования

### Use Case 1: Разные IP для разных доменов

```
IP Pool: "Marketing Emails"
  - IP1 (192.168.1.10) → Proxy → 203.0.113.50
  - IP2 (192.168.1.11) → Proxy → 203.0.113.51

IP Pool: "Transactional Emails"
  - IP3 (192.168.1.20) → Direct (no proxy)
```

### Use Case 2: IP Rotation для warming

```
IP Pool: "New IPs"
  - IP1 Priority 10  → Proxy → 203.0.113.60 (новый, прогрев)
  - IP2 Priority 100 → Proxy → 203.0.113.61 (прогретый)
```

### Use Case 3: Geo-distributed sending

```
IP Pool: "USA"
  - IP1 → Proxy → USA_SERVER_IP

IP Pool: "EU"
  - IP2 → Proxy → EU_SERVER_IP
```

---

## 🔮 Roadmap

- [ ] SSH key authentication (вместо пароля)
- [ ] Поддержка HTTP CONNECT proxy
- [ ] Proxy health monitoring
- [ ] Автоматический failover при падении прокси
- [ ] Proxy pools (ротация между несколькими прокси)
- [ ] WireGuard VPN integration
- [ ] Docker compose для быстрого деплоя прокси серверов

---

## ❓ FAQ

**Q: Можно ли использовать один прокси для нескольких IP?**
A: Да! Просто укажите один и тот же proxy_host для разных IP addresses.

**Q: Будет ли работать с IPv6?**
A: Да, если SOCKS proxy поддерживает IPv6.

**Q: Можно ли использовать внешний SOCKS сервис (коммерческий прокси)?**
A: Да! Используйте Manual mode и укажите данные от провайдера.

**Q: Как удалить Dante с удаленного сервера?**
A: SSH туда и выполните: `apt-get remove dante-server` или `yum remove dante-server`

**Q: Какая задержка добавляется прокси?**
A: Обычно 10-50ms в зависимости от расстояния между серверами.

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи установки в интерфейсе
2. Проверьте Rails логи: `/opt/postal/log/production.log`
3. Проверьте SMTP логи: `/opt/postal/log/smtp.log`
4. Проверьте статус Dante на удаленном сервере

---

## 🎉 Заключение

Теперь у вас есть полноценная система управления SOCKS прокси прямо в веб-интерфейсе Postal!

**Главное преимущество:** Не нужно копаться в конфигах, SSH, и терминале. Всё делается в 5 кликов через веб-интерфейс.

**Happy Mailing!** 📧🚀
