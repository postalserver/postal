- page_title << "IP Pools"
- page_title << @ip_pool.name
- page_title << "Install Proxy on Other Server"
.pageHeader
  %h1.pageHeader__title
    %span.pageHeader__titlePrevious
      = link_to "IP Pools", :ip_pools
      &rarr;
      = @ip_pool.name
      &rarr;
      Edit
      &rarr;
    Install Proxy on Other Server

.pageContent.pageContent--compact
  = render 'form_install'
